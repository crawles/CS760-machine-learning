naive bayes and TAN
    * binary
    * discrete attributes
    * Laplace estimates all probabilities

TAN
    * Prim's algorithm, maximal spanning tree, /maximal/ weight edges
    * should determine network structure, model parameters, classification
        * structure?

ARFF
    * use previous code, except 
    *  

might be good to first test on naive bayes

Naive Bayes:

P(class|attributes) = p(attributes|class)p(class)/p(attributes)
    p(attributes|class) = p(attributes,class)/p(class)

